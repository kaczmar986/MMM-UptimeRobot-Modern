stages:
  - test

test-20:
  stage: test
  image:
    name: docker-registry.wikimedia.org/releng/node20-test-browser:latest
    entrypoint: [ "" ]
  script:
    - node --version
    - npm --version
    - npm ci
    - npm test
